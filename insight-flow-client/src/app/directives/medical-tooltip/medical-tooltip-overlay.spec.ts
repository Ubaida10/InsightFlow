import { MedicalTooltipOverlay } from './medical-tooltip-overlay';

describe('MedicalTooltipOverlay', () => {
  it('should create an instance', () => {
    const directive = new MedicalTooltipOverlay();
    expect(directive).toBeTruthy();
  });
});
